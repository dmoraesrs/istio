FROM ubuntu:20.04

# Configurações para evitar prompts interativos
ENV DEBIAN_FRONTEND=noninteractive

# Adiciona o repositório 'universe' e atualiza o apt-get
RUN apt-get update && apt-get install -y software-properties-common && \
    add-apt-repository universe && \
    apt-get update

# Instalação de pacotes e ferramentas essenciais
RUN apt-get upgrade -y && \
    apt-get install -y -qq --no-install-recommends \
    apt-transport-https \
    apt-utils \
    ca-certificates \
    curl \
    git \
    iputils-ping \
    jq \
    lsb-release \
    software-properties-common \
    libicu-dev \
    unzip \
    gettext \
    python3-pip \
    npm \
    python3-setuptools \
    python3-venv && \
    rm -rf /var/lib/apt/lists/*

# Resto da configuração
RUN curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip" && \
    unzip awscliv2.zip && ./aws/install && rm -rf awscliv2.zip aws

RUN curl -fsSL -o get_helm.sh https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3 && \
    chmod +x get_helm.sh && ./get_helm.sh && rm get_helm.sh

RUN curl -sL https://aka.ms/InstallAzureCLIDeb | bash

RUN curl -LO "https://dl.k8s.io/release/$(curl -L -s https://dl.k8s.io/release/stable.txt)/bin/linux/amd64/kubectl" && \
    mv kubectl /usr/local/bin/kubectl && chmod +x /usr/local/bin/kubectl

RUN curl -fsSL http://get.docker.com | bash

# Define o diretório de trabalho
WORKDIR /azp
